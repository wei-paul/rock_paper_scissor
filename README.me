# Type speed test 
- should show the user a random text and measure the time user will type the same text as an input, the app should be able to find the mistypes the user is making, and include them in the final result.
- Random text

- Timer
